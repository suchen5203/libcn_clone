name: build-libcn-clone

on:
  push:
    branches: [main, master]
    paths:
      - 'src/**'
      - 'docs/**'
      - 'Makefile'
      - 'Dockerfile'
      - 'build.*'
      - 'smoke_test.sh'
      - 'push*.ps1'
      - 'push*.sh'
      - 'push*.bat'
      - '.github/workflows/build.yml'
  pull_request:
    paths:
      - 'src/**'
      - 'Makefile'
      - '.github/workflows/build.yml'
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python (for analysis scripts)
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install capstone (reverse tooling)
        run: pip install capstone

      - name: Install 32-bit toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            libc6-dev \
            libc6-dev-i386 \
            libssl-dev \
            file \
            make

      - name: Build libcn_clone.so
        run: |
          make clean
          make USE_OPENSSL=1
          make check

      - name: Verify ELF output
        run: |
          file libcn_clone.so
          ls -la libcn_clone.so

      - name: Quick smoke test (load + check exports)
        run: |
          chmod +x smoke_test.sh
          ./smoke_test.sh || echo "(smoke test 跳过,需 32 位环境)"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: libcn_clone
          path: |
            libcn_clone.so
            data/*.data
          if-no-files-found: warn
          retention-days: 14

      - name: Build summary
        if: success()
        run: |
          echo "### 构建成功" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "产物:**libcn_clone.so**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "从 Actions 页 Artifacts 区域下载。" >> $GITHUB_STEP_SUMMARY